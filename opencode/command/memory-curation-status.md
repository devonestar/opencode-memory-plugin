---
description: Show redacted automatic memory curation status, eligibility metrics, and report availability.
agent: build
---

Call `memory_curation_status` exactly once with `{}`. Return that tool result and call no other tool.
