---
description: Resume automatic and manual memory curation.
agent: build
---

Call `memory_curation_control` exactly once with `{"action":"resume"}`. Return that tool result and call no other tool.
