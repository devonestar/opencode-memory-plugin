---
description: Force an asynchronous memory curation run that applies only locally validated safe operations.
---

Call `memory_curation_run` exactly once with `{"dryRun":false}`. Return that tool result and call no other tool.
